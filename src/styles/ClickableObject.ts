import { StyleSheet } from 'react-native';

export const ClickableStyles = StyleSheet.create({
    clickableText: {
        color: 'blue',
        textDecorationLine: 'underline',
        fontWeight: 'bold'
    },
    clickableLogo: {
        width: 100,
        height: 100,
        resizeMode: 'contain'
    }
});